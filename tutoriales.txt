//ver variables en heroku
heroku config

//Crear variables en heroku
heroku config:set name="claudios"
heroku config:set MONGODB_CNN="mongodb+srv://cgarcete:Z98xFKyAccKJbadZ@miclustercafe.m0xlh.mongodb.net/test"

//borrar variabl en heroku
heroku config:unset nombreVar

//logs
heroku logs -n 10
//mantener logs en vivo
heroku logs -n 10 --tail 


// borrar un archivo del repositorio de git
git rm .env --cached

//Crear en BD CLIENTES, document: roles
{
  "rol": "ADMIN_ROLE"
}

// Conectarse a mongodb server de docker
docker exec -it jolly_nash mongo
use Clientes
db.roles.insertOne({rol:"ADMIN_ROLE"})
db.roles.insertOne({rol:"USER_ROLE"})
// db.createCollection("test_collection")


